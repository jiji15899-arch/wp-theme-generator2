# Cloudflare Pages 배포 설정
# 제작: 아백 (아로스)
# 생성일: 2026-01-09

name = "wp-theme-generator2"
compatibility_date = "2026-01-09"

[assets]
directory = "."
html_handling = "auto-trailing-slash"

# 404 페이지 설정
[[assets.fallback_routes]]
pattern = "/*"
destination = "/index.html"
